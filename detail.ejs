<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= drama.series_title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white">
    <div class="p-4 flex items-center bg-gray-900 sticky top-0 z-50">
        <button onclick="history.back()" class="mr-4 text-blue-400">← Kembali</button>
        <h1 class="font-bold truncate"><%= drama.series_title %></h1>
    </div>

    <div class="p-4 max-w-2xl mx-auto">
        <img src="<%= drama.series_cover %>" class="w-full rounded-2xl shadow-lg mb-6 border border-gray-800">
        
        <h2 class="text-2xl font-bold mb-2"><%= drama.series_title %></h2>
        <p class="text-sm text-gray-400 mb-6 leading-relaxed"><%= drama.series_intro %></p>

        <h3 class="font-bold text-lg mb-4">Episode (<%= drama.episode_cnt %>)</h3>
        <div class="grid grid-cols-1 gap-3">
            <% drama.video_list.forEach((vid, index) => { %>
                <a href="/stream/<%= vid.video_id || index %>" class="bg-gray-900 p-4 rounded-xl flex items-center justify-between border border-gray-800 hover:bg-gray-800">
                    <div class="flex items-center">
                        <div class="w-10 h-10 bg-blue-600 rounded-full flex items-center justify-center mr-4 font-bold">
                            <%= index + 1 %>
                        </div>
                        <span class="text-sm font-medium">Episode <%= index + 1 %></span>
                    </div>
                    <span class="text-blue-500 text-xs">Nonton →</span>
                </a>
            <% }) %>
        </div>
    </div>
</body>
</html>

