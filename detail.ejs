<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="referrer" content="no-referrer">
    <title><%= drama.series_title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white">
    <div class="sticky top-0 bg-black/90 p-4 border-b border-gray-800 flex items-center z-50">
        <button onclick="history.back()" class="text-blue-500 mr-4 font-bold text-xl">←</button>
        <h1 class="font-bold truncate text-sm"><%= drama.series_title %></h1>
    </div>

    <div class="max-w-2xl mx-auto">
        <div class="w-full aspect-[3/4] relative bg-gray-900">
            <img src="<%= drama.series_cover || drama.cover %>" referrerpolicy="no-referrer" class="w-full h-full object-cover">
            <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent"></div>
        </div>

        <div class="p-6">
            <h2 class="text-2xl font-black mb-2 uppercase"><%= drama.series_title %></h2>
            <p class="text-[10px] bg-blue-600 inline-block px-2 py-1 rounded font-bold mb-4">
                <%= drama.episode_right_text %>
            </p>
            
            <h3 class="text-lg font-bold mb-2 text-gray-300">SINOPSIS</h3>
            <p class="text-gray-400 text-sm leading-relaxed mb-8">
                <%= drama.series_intro %>
            </p>

            <h3 class="text-lg font-bold mb-4">DAFTAR EPISODE</h3>
            <div class="grid grid-cols-1 gap-3">
                <% if(drama.video_list && drama.video_list.length > 0) { %>
                    <% drama.video_list.forEach((vid, index) => { %>
                        <a href="/stream/<%= vid.video_id %>" class="flex gap-4 bg-gray-900 p-3 rounded-xl border border-gray-800">
                            <div class="w-24 h-16 bg-gray-800 rounded-lg overflow-hidden shrink-0">
                                <img src="<%= vid.episode_cover || drama.series_cover %>" referrerpolicy="no-referrer" class="w-full h-full object-cover">
                            </div>
                            <div class="flex flex-col justify-center">
                                <p class="text-sm font-bold">Episode <%= index + 1 %></p>
                                <p class="text-[10px] text-blue-500 font-bold uppercase">Nonton Sekarang</p>
                            </div>
                        </a>
                    <% }) %>
                <% } else { %>
                    <p class="text-gray-600 italic">Daftar episode tidak ditemukan.</p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>

