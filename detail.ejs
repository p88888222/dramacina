<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= drama.title %> - Detail</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white">
    <div class="p-4 flex items-center border-b border-gray-800 sticky top-0 bg-black/90 z-50">
        <button onclick="history.back()" class="mr-4 text-gray-400">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
            </svg>
        </button>
        <h1 class="text-lg font-bold truncate"><%= drama.title %></h1>
    </div>

    <div class="max-w-4xl mx-auto p-4">
        <div class="flex flex-col md:flex-row gap-6 mb-8">
            <img src="<%= drama.image %>" class="w-full md:w-64 rounded-xl shadow-2xl">
            <div class="flex-1">
                <h2 class="text-3xl font-bold mb-2"><%= drama.title %></h2>
                <div class="flex gap-2 mb-4">
                    <span class="bg-blue-600 px-2 py-1 rounded text-xs">Full HD</span>
                    <span class="bg-gray-800 px-2 py-1 rounded text-xs text-gray-300">Drama Short</span>
                </div>
                <p class="text-gray-400 text-sm leading-relaxed">
                    <%= drama.description || 'Tidak ada deskripsi tersedia untuk drama ini.' %>
                </p>
            </div>
        </div>

        <div class="space-y-3">
            <h3 class="text-xl font-bold mb-4">Daftar Episode</h3>
            <% drama.episodes.forEach((ep) => { %>
                <a href="/stream/<%= ep.id %>" class="flex items-center justify-between bg-gray-900 p-4 rounded-lg hover:bg-gray-800 border border-gray-800 transition">
                    <span class="font-medium text-gray-200"><%= ep.title %></span>
                    <div class="flex items-center text-blue-500 text-sm">
                        <span class="mr-2">Nonton</span>
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z" />
                        </svg>
                    </div>
                </a>
            <% }) %>
        </div>
    </div>
</body>
</html>
