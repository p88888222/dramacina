<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="referrer" content="no-referrer">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white h-screen flex flex-col">
    <div class="p-4 flex items-center">
        <button onclick="history.back()" class="text-blue-500 mr-4 font-bold text-xl">←</button>
        <span class="text-xs font-bold text-gray-400">VIDEO PLAYER</span>
    </div>
    
    <div class="flex-1 flex items-center justify-center p-4">
        <div class="w-full max-w-4xl aspect-video bg-gray-900 rounded-2xl overflow-hidden shadow-2xl">
            <% if(video.main_url) { %>
                <video src="<%= video.main_url %>" controls class="w-full h-full" poster="<%= video.cover %>"></video>
            <% } else { %>
                <div class="flex items-center justify-center h-full text-gray-500 italic text-sm">Link video tidak tersedia atau kadaluarsa</div>
            <% } %>
        </div>
    </div>
    
    <div class="p-6 text-center">
        <p class="text-[10px] text-gray-600">Gunakan browser terbaru jika video tidak berputar.</p>
    </div>
</body>
</html>

