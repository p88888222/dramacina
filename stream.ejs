<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="referrer" content="no-referrer">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white h-screen flex flex-col items-center justify-center p-4">
    <h2 class="mb-4 text-sm font-bold text-gray-400"><%= stream.title %></h2>
    <div class="w-full max-w-4xl aspect-video bg-gray-900 rounded-2xl overflow-hidden shadow-2xl">
        <iframe src="<%= stream.url %>" class="w-full h-full" allowfullscreen frameborder="0"></iframe>
    </div>
    <button onclick="history.back()" class="mt-8 bg-blue-600 px-8 py-2 rounded-full font-bold">KEMBALI</button>
</body>
</html>

